class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int start = 0;
        for (auto num : nums) {
            if (num != val)
                nums[start++] = num;
        }
        return start;
    }
};
